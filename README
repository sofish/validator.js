validator.js
============

HTML5表单验证插件


当前目标

* jQuery插件，即插即用
* 基于HTML5的API
* 兼容 IE 6+

可能的未来目标

* 按需加载
* 很好的支持自定义错误提示
* 兼容手机浏览器
* 兼容采用jQuery API的其他库（如Zepto）
* 支持capture事件（主要为了解决invalid等事件不bubble的问题）
